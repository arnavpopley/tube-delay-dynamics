# Secrets — never commit a live key
.env

# Append-only research data and derived tables. Raw is irreplaceable;
# processed is regenerated by src/ingest.py.
data/

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.ipynb_checkpoints/

# Generated artefacts (rebuild with python src/ingest.py / run_all.py)
results/*
!results/.gitkeep

# Editors / OS
.DS_Store
.idea/
.vscode/

# Website build
web/node_modules/
web/dist/

# Cloud-agent drop folder, not part of the research repo
uploads/

# Working clone used to publish public heartbeat JSON (not research data)
.heartbeat-git/
collector/deploy/heartbeat_deploy_key
