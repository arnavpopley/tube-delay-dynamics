# Agent instructions

Read `PROJECT.md` first. It is the standing research brief.

Then read `.cursorrules` for the hard stops (phase order, no reconstruction
yet, non-negotiables).

Do not invent later-phase modules. Empty stubs for `reconstruct.py` and
downstream scripts are worse than absence — they invite logic written
before anyone has seen real sequences.
